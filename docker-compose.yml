version: '3.7'
services:
  postgres:
    image: postgres:10.5
    restart: always
    environment:
      - POSTGRES_USER=proot
      - POSTGRES_PASSWORD=proot
    ports:
      - '5438:5432'
    volumes:
      - ./postgres-data:/var/lib/postgresql/data